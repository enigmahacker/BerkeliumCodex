#!/usr/bin/env bash
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
if [ -f "$DIR/apps/cli/dist/bin/berkelium.js" ]; then
  exec node "$DIR/apps/cli/dist/bin/berkelium.js" "$@"
else
  exec npx tsx --tsconfig "$DIR/tsconfig.base.json" "$DIR/apps/cli/src/bin/berkelium.ts" "$@"
fi
